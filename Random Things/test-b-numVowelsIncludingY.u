#include "labfunctions.h"

test suite numVowelsIncludingY
{
tests:
  test simpleNoY
  {
    check (numVowelsIncludingY("test phrase")) expect == 3;
  }

  test simpleWithY
  {
    check (numVowelsIncludingY("yes no why not?")) expect == 4;
  }
}
