#include "labfunctions.h"

test suite getInitials
{
tests:
  test simple
  {
    check (getInitials("Internal Revenue Service")) expect == "IRS";
    check (getInitials("Associated Press")) expect == "AP";
  }
}
