#include "labfunctions.h"

test suite lastVowel
{
tests:
  test simpleWithVowel
  {
    check (lastVowel("Longwood")) expect == 'o';
  }

  test noVowel
  {
    check (lastVowel("nvrst")) expect == '!';
  }
}
