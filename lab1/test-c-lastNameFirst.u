#include "labfunctions.h"

test suite lastNameFirst
{
tests:
  test simple
  {
    check (lastNameFirst("Don Blaheta")) expect == "Blaheta, Don";
    check (lastNameFirst("Scott McElfresh")) expect == "McElfresh, Scott";
  }
}
